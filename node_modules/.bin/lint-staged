#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_lint-staged@13.0.3@lint-staged/bin/lint-staged.js" "$@"
  ret=$?
else 
  node  "$basedir/../_lint-staged@13.0.3@lint-staged/bin/lint-staged.js" "$@"
  ret=$?
fi
exit $ret
